<div class="card my-3 ">
  <h5 class="card-header">
    <% if @message.outbox.user.is_doctor? %>Dr.<% end %>
    <%= @message.outbox.user.full_name %>
  </h5>
  <div class="card-body">
    <p class="card-text"><%= @message.body %></p>
    <%= link_to "Reply", new_message_path(:original_msg => @message.id), class: "btn btn-primary" %>
    <%= link_to "I've lost my prescription",  messages_path(:message => {:body => "I've lost my script, please issue a new one at a charge of €10", :inbox_id => User.admin.first, :outbox_id => User.current, :payment => TRUE}), :method => :post, class: "btn btn-primary" %>
  </div>
</div>